import type {
  BetDTO,
  BetStatus,
  BetMarketType,
  MarketOddsDTO,
  SelectionOdds,
  LeaderboardEntryDTO,
} from '@betting/core'

export type { BetDTO, BetStatus, BetMarketType, MarketOddsDTO, SelectionOdds, LeaderboardEntryDTO }
