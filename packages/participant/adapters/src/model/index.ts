// Rich entity re-exported as a VALUE: the app's Prisma repository reconstitutes
// it (`new Participant({...})`) without importing @participant/core. Adapters
// is the context's only public surface.
export { Participant } from '@participant/core'
