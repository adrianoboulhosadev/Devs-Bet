import type {
  PollRepository,
  PollQueryRepository,
  PollCloseQueue,
  PollCloseCommand,
} from '@poll/core'

export type { PollRepository, PollQueryRepository, PollCloseQueue, PollCloseCommand }
